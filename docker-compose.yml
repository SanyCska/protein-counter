services:
  bot:
    build: .
    image: protein-counter-bot:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PROTEIN_DB_PATH: /app/data/protein.sqlite3
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
